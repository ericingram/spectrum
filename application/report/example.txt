Что должно в итоге получиться.

context('Земля', function(){});
context('Вода', function(){});
it('Должен работать', function(){ be(true)->true(); });

Экспорт спеков:
	$spec->export->getReportBufferWithDeclaringStructure()->asPlain(); // asXhtml(), asXml()
	Земля
	Вода
	Должен работать

	$spec->export->getReportBufferWithRunningStructure()->asPlain();
	Земля
		Должен работать
	Вода
		Должен работать

Сохранение отчета выполнения
	$buffer = new Buffer();
	$spec->runReport->addBufferForFilling($buffer);
	$spec->run();

	$buffer->asXml(); // asPlain(), asXhtml(), asFlatXml
		<specs>
			<context>
				<name>Земля</name>
				<result>success</result>
				<specs>
					<it>
						<name>Должен работать</name>
						<result>success</result>
					</it>
				</specs>
			</context>

			<context>
				<name>Вода</name>
				<result>success</result>
				<specs>
					<it>
						<name>Должен работать</name>
						<result>success</result>
					</it>
				</specs>
			</context>
		<specs>


Отчет о выполнении в реальном времени
	$spec->liveReport->setTemplate('Xhtml'); // Или liveRunReport